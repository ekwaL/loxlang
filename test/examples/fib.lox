fun fif(b) {
    print "fif( " + b + ")";
    if (b < 0) return;
    fif(b - 1);
    fif(b - 2);
}

fun zif(b) {
    print "fif( " + b + ")";
    bif(b - 1);
    bif(b - 2);
    bif(b - 3);
    bif(b - 4);
}

fun bif(b) {
    print "bif( " + b + " )";
}

zif(3);


var y = 20;

fun p(y) {
    bif(y);
    bif(y-2);
    bif(y-4);
    // print y;
    // print y-2;
    // print y -4;
}

p(y-2);
p(y-2);
p(y-2);
p(y-2);
p(y-2);
